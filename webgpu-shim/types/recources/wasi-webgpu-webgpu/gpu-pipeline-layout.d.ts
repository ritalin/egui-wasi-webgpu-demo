import peer = globalThis;
export declare class GpuPipelineLayout {
    private readonly _handle;
    constructor(_handle: peer.GPUPipelineLayout);
    label(): string;
    setLabel(label: string): void;
}
