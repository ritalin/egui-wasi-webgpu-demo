import peer = globalThis;
export declare class GpuBindGroupLayout {
    readonly _handle: peer.GPUBindGroupLayout;
    constructor(_handle: peer.GPUBindGroupLayout);
    label(): string;
    setLabel(label: string): void;
}
