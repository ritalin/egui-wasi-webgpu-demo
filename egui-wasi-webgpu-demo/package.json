{
  "name": "egui-wasi-webgpu-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:shim": "pnpm --filter wasi-webgpu-runtime run build",
    "build:pkg": "./scripts/transpile.sh",
    "build:dist": "tsc && vite build",
    "build": "pnpm run build:shim && pnpm run build:pkg release && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.16.1",
    "@types/node": "^25.2.3",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@bytecodealliance/preview2-shim": "^0.17.8",
    "wasi-webgpu-runtime": "link:../wasi-webgpu-runtime"
  }
}
